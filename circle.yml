machine:
  environment:
    PATH: "$HOME/.asdf/bin:$HOME/.asdf/shims:$PATH"
    MIX_ENV: "test"
    REL_NAME: "foodbot"

dependencies:
  cache_directories:
    - ~/.asdf
    - deps
  pre:
    - if ! asdf | grep version; then git clone https://github.com/HashNuke/asdf.git ~/.asdf; fi
    - if ! asdf plugin-list | grep erlang; then asdf plugin-add erlang https://github.com/HashNuke/asdf-erlang.git; fi
    - if ! asdf plugin-list | grep elixir; then asdf plugin-add elixir https://github.com/HashNuke/asdf-elixir.git; fi
    - asdf install
    - yes | mix local.rebar
    - yes | mix deps.get

test:
  override:
    - mix test

deployment:
  production:
    branch: master
    commands:
      - MIX_ENV=prod mix compile
      - MIX_ENV=prod mix release --env=prod
      - script/deploy.sh "$REL_NAME" "$SSH_SERVER" "$DEPLOY_PATH"
